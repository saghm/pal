int sum3(x, y, z) {
    return x + y + z;
}

void print_sums(n) {
   let a = range(n, 0);
   print "Printing ";
   print length(a);
   print_line " sums:";

    for i in a {
        print_line sum3(i, i + 1, i + 2);
    }
}

print_sums(10);
